/**
 * Layout untuk halaman publik (tanpa sidebar/header dashboard).
 * Digunakan untuk form pendaftaran TFT dan form publik lainnya.
 */
export default function PublicFormLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
